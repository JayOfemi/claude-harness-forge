# Your projects live here, one folder per project.
#
# Paste or clone a project into Projects/<Name>/<repo>/, then open a session at
# your workspace root and say: onboard <Name>. The onboarding skill reads the
# code, writes its tracker, and changes nothing inside your files.
